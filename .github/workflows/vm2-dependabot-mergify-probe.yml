name: VM2 Dependabot Mergify secret boundary probe
on:
  pull_request:
permissions:
  contents: read
jobs:
  probe:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest
    env:
      MERGIFY_TOKEN: ${{ secrets.MERGIFY_TOKEN }}
    steps:
      - name: Run controlled external Action
        uses: livastic-vm2-secondary-20260911/vm2-dep-action-0914-k7m3@v1.0.3

